abbr -a -g -- cat 'bat'
abbr -a -g -- ls eza
abbr -a -g -- exg 'eza -lh --git'
abbr -a -g -- k kubectl
abbr -a -g -- kn 'kubectl config set-context --current --namespace'
abbr -a -g -- kc 'kubectl config use-context'
abbr -a -g -- ta 'task add'
abbr -a -g -- v nvim
abbr -a -g -- view 'nvim -R'
abbr -a -g -- vs 'nvim -S Session.vim'
abbr -a -g -- ps procs
abbr -a -g -- ghpr 'gh pr create --title (bjtitle)'
abbr -a -g -- ghpd 'gh pr create --title (bjtitle) --draft'
abbr -a -g -- ghmd 'gh pr merge --auto --merge --delete-branch'
abbr -a -g -- ghprmd 'gh pr create --title (bjtitle) --reviewer ymeadows/backend; and gh pr merge --auto --merge --delete-branch'
abbr -a -g -- bc 'kalker'
abbr -a -g -- kk 'kalker'
